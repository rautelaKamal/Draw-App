# Copy to .env before running docker compose:
#   cp .env.example .env
#
# Both backends must sign and verify with the same secret, or a token issued
# by http-backend will be rejected by ws-backend. Generate a real one with:
#   openssl rand -base64 32
JWT_SECRET=dev-only-change-me
